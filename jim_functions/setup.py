from setuptools import find_packages, setup

setup(
    name='jim_functions',
    packages=find_packages(include=['jim_functions']),
    #install_requires=['pandas', 'numpy'],
    version='0.1.0',
    description='My first Python library',
    author='Jim Cody',
)
